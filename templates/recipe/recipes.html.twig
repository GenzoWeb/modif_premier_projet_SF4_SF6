{% extends 'base.html.twig' %}

{% block title %}Recettes{% endblock %}

{% block body %}
   <div class="jumbotron">
      <div class="container">
         {{ include('recipe/_searchRecipes.html.twig') }}
      </div>
   </div>
   <div class="container recipes mt-4">
      {% if not recipes.items %}
         <p class="text-center">Il n'y a pas de recette qui correspond à votre recherche</p>
      {% else %}
         <h1 class="text-center">Recette{% if recipes| length > 1 %}s{% endif %}</h1>
         {{ include('recipe/_listRecipes.html.twig') }}
      {% endif %}
   </div>
   <div class="pagination d-flex justify-content-center">
      {{ knp_pagination_render(recipes) }}
   </div>
{% endblock %}
